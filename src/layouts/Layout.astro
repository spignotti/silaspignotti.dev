---
import '@/styles/global.css'
import '@/styles/typography.css'

import Footer from '@/components/Footer.astro'
import Head from '@/components/Head.astro'
import Navbar from '@/components/react/navbar'
import { withBasePath } from '@/lib/paths'
import { SITE } from '@/consts'
import { cn } from '@/lib/utils'

const {
  isWide = false
} = Astro.props

---

<!doctype html>
<html lang={SITE.locale}>
  <Head>
    <slot name="head" />
    <script is:inline src="https://analytics.ahrefs.com/analytics.js" data-key="+FHMgRP7/Duxaq5D0gZtJw" async></script>
    
    <!-- Preload critical resources -->
    <link rel="preload" href={withBasePath('/fonts/ClashDisplay/ClashDisplay-Semibold.woff2')} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={withBasePath('/fonts/Lexend/Lexend-Regular.woff2')} as="font" type="font/woff2" crossorigin />

    <!-- DNS prefetch for external resources -->
    <link rel="dns-prefetch" href="//analytics.ahrefs.com" />
    
    <script is:inline data-astro-rerun>
      (function() {
        try {
          const stored = localStorage.getItem('theme');
          const media = window.matchMedia('(prefers-color-scheme: dark)');
          const applyDark = (isDark) => document.documentElement.classList.toggle('dark', isDark);

          if (stored === 'dark' || stored === 'light') {
            applyDark(stored === 'dark');
          } else {
            applyDark(media.matches);
            media.addEventListener('change', (e) => {
              if (!localStorage.getItem('theme')) applyDark(e.matches);
            });
          }
        } catch (error) {
          console.warn('Theme setting failed:', error);
        }
      })();
    </script>
  </Head>
  <body>
    <div class="flex h-fit min-h-screen w-full flex-col gap-y-4 sm:gap-y-6 font-sans">
      <Navbar client:load />
      <main class="flex grow flex-col bg-background mt-16 lg:mt-6" aria-label="Main content" role="main">
        <div
          class={cn(
            'mx-auto flex grow flex-col gap-y-4 sm:gap-y-6 px-3 py-10 sm:py-16 md:py-20 lg:py-24 sm:px-6 lg:px-8',
            !isWide && 'max-w-7xl'
          )}
        >
          <slot />
        </div>
      </main>
      <Footer />
    </div>
  </body>
</html>
